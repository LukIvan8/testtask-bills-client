import styled from 'styled-components';
import {IonApp} from '@ionic/react';

export const StyledIonApp = styled(IonApp)`
  --ion-background-color: none;
  --background: none;
  background: linear-gradient(to bottom, #FBF3F4, #B8D0E8);
`;



